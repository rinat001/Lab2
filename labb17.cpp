#include <iostream>
#include <cmath>

using namespace std;

int main ()
{
	int x, y, z;
	double a, b, e=2.7;
	cin >> x >> y >> z;
	a = (sqrt(abs(x - 1)) - pow(abs(y, 1 / 3))) / (1 + x * x / 2 + y * y / 4);
	b = x * (atan(z) + powl(e,(- (x + 3))));
	cout <<a << " " << b;  

return 0;
}

